require 'spec_helper'

describe ProgressTracker do
  it 'has a version number' do
    expect(ProgressTracker::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
